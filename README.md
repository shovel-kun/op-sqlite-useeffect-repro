# op-sqlite-useeffect-repro

## Getting Started

Install the dependencies:

```bash
npm install
```

Then, prebuild the native code:

```bash
npx expo prebuild
```

Then, run the app:

```bash
# Android
npm run android

# iOS
npm run ios
```
